[build]
builder = "dockerfile"
dockerfile = "Dockerfile"

[deploy]
startCommand = "python -m gunicorn app:app --worker-class=gthread --workers=2 --threads=2 --bind 0.0.0.0:8080"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
